import { systemLang } from "../../global"

const strings = systemLang() == 'ko' ? 
    {
        "lno" : "L&O",
        "new" : "NEW"
    } : {
        "lno" : "L&O",
        "new" : "NEW"
    }

export default strings
